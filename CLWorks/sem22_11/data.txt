p21 && p22 - --//-- завершился успешно
p21 || p22 - второй делается, если первый завершился не успешно

в shell приоритет || и && одинаков

Пример: мы не закрыли лишний дескриптор чтения в отце - тогда сын при чтении не
получит конец файла и будет блокировка

pipe могут использовать те процессы, которые имеют родственника,
от которого они унаследовали канал

() создают subshell в bush
{} - изменение приоритета операций - сначала что в скобках

echo $SHELL - посмотреть, какой у тебя шелл

СИГНАЛЫ
- как бы программные прерывания; их может генерировать сама вычислительная система
после завершения по сигналу остается зомби 
kill(pid, nsig) - кому, чем

в старых версиях сигналы, которые идут друг за другом могли перекрываться
sigpipe - убивает процесс, который должен был зависнуть на чтении
<signal.h>

signal();
короче, файл "СИГНАЛЫ и КАНАЛЫ"

оброботчики наследуются


зачет планируем на 24, 20 - зачетная контрольная
27 - второй день зачета - время согласовать с Владимиром Боблевым
24 и 27 - это шанс досдать только по практике 